# Preflight 10 — adjudication of the converged finding set

# Adjudication Report — `frogg3rs-delay-width-wysiwyg-repair`

Verified by opening the artifacts directly (repo at `/Users/diegoaguilar-canabal/Desktop/frogg3rs`, change directory `openspec/changes/frogg3rs-delay-width-wysiwyg-repair/`). `git status --short` confirms no production code has changed: only `HANDOFF.md`, this change's `proposal.md`/`tasks.md` are modified, and `research/`, `frogg3rs-midi-controller-resilience/`, `frogg3rs-randomize-depth-reclaim/` are untracked. `app/dsp/Delay.hpp` and `app/FroggersDspParityTests.cpp` are unmodified from what ships today.

## C1 — FALSE as stated (diagnosis sound, number wrong)

Ran the actual gate exactly as `app/Makefile`'s `check-spec-checks-resolve` target invokes it:
```
$ python3 app/check_spec_checks_resolve.py app
check-spec-checks-resolve: OK - 67 Check reference(s) resolved, 10 declared as having no automated check
```
Exit 0, **zero** unresolvable names — not three, not the two `proposal.md`/`tasks.md`/`HANDOFF.md` assert. Read the actual delta: `openspec/changes/frogg3rs-randomize-depth-reclaim/specs/froggers-modulation-slate/spec.md`'s four `Check:` lines all read `Not yet delivered. Task N creates the case...` — every one matches the gate's own `NO_CHECK` regex (`^\s*(none|operator step|not yet delivered)`) and passes trivially; none of them cite a nonexistent test name. `stat` shows that file was modified at 23:10:59, thirteen minutes before my 23:23:03 check — i.e., the concurrent session the proposal warns about edited exactly this file, in exactly the window between when the "two failures" claim was written and when I read it. The `frogg3rs-midi-controller-resilience` spec's two `Check:` lines both cite `device_defaults_are_valid_and_address_exactly_the_documented_controls`, which exists verbatim at `app/FroggersMidiCatalogTests.cpp:397` — also resolves.

So the specific count-and-name claim in the documents is **already stale**, and C1's own replacement count ("three") is **also wrong** — the true count right now is zero. The general diagnosis — a rule stated as a literal count-and-name list will drift under a concurrently-edited directory this change doesn't own, and a path-scoped exclusion (ignore any failure whose file lives under `frogg3rs-randomize-depth-reclaim/`) would not — is sound and demonstrated by the very drift I just caught. **Consequence:** neither, right now — the gate is green so nothing is blocked. But `tasks.md`'s stage-gate wording anchors on "two named exclusions"; an executor should re-measure fresh at each stage gate (which `tasks.md`'s own text technically permits — "confirm the count has not grown" is looser than the "exactly two, named X" framing elsewhere) rather than trust the proposal's stated count. Recommend adopting the path-scoped exclusion C1 proposes, as a robustness fix, not because today's number is wrong in the documents' favor.

## C2 — TRUE

Promoted spec (`openspec/specs/froggers-sheaf-parameter-model/spec.md:106-111`, identical verbatim in the change's own delta at lines 106-111) reads: **THEN** "L/R correlation of that stage's wet signal falls monotonically." `proposal.md`'s "The check" section (lines 149-163) states explicitly: "The check asserts the repaired law's `|corr|` stays under such a bound **at every grid point**... This gives up the relative shape's self-documenting 'strictly more decorrelated than today' statement at runtime." `tasks.md` 1.1b: "Assert `repairedCorr < <bound>` at every grid point" — no row-to-row comparison anywhere. **Consequence:** blocks ARCHIVAL. Task 3.1 only instructs repointing the `Check:` line and correcting the "two mechanisms" diagnosis text — it does not instruct rewriting the THEN clause's monotonicity language. Left as-is, the archived requirement asserts monotonic falling while its own cited check only asserts an absolute bound — a scenario-vs-check mismatch of exactly the kind `check-spec-checks-resolve`'s existence proves this project cares about, except this gate doesn't parse prose so it won't catch it. Task 3.1 needs explicit scope to fix the THEN clause (or send the wording change to the operator, since "editing a requirement to match an implementation is forbidden" per this document's own house rule).

## C3 — TRUE

Grepped `proposal.md`/`tasks.md` for concrete grid values: the word "grid" appears seven times, always as "the Stereo width grid" / "across the grid" — never as an actual list of swept values (contrast the *old*, superseded five-point grid `{0.0, 0.25, 0.5, 0.75, 1.0}` quoted in the promoted spec for a different, retired measurement). The instrument section (`proposal.md` lines 165-197) pins stimulus, statistic, operating point, tap point, companion assertion, and even states "Stereo width at 0... reads a flat +1 — void, not negative, and never asserted as a discriminating point," but never says whether width=0 is *excluded from the max-taking* that sets the bound, only that it's not one of the asserted rows. `Correlation::Value()` (`app/FroggersDspParityTests.cpp:9500`) is mathematically capped at `|corr| <= 1.0`; if 1.0 (width 0) enters "measured maxima across the grid" un-excluded, the derived bound is ≥1.0, and 1.1a's own required sanity check ("confirm at least one grid point exceeds it" against the restored old formula) becomes unsatisfiable by construction. **Consequence:** blocks EXECUTION of task 1.1a. It cannot be run as literally written without an executor silently deciding the grid's exact points and whether width=0 is excluded — exactly the kind of invented scope `tasks.md`'s Mechanics section forbids ("no blank for that task to fill"). Must be fixed before 1.1a is dispatched.

## C4 — TRUE

`tasks.md` lines 73 and 75, verbatim: `Assert repairedCorr < <1.1a's correlation bound> at every grid point` and `|rmsL - rmsR| / (rmsL + rmsR) < <1.1a's level-balance bound>`. These are literal, unfilled angle-bracket placeholders in the file as it stands today. **Consequence:** blocks EXECUTION outright. This is a direct violation of `tasks.md`'s own stated rule two paragraphs above ("No task hands an executor a check without its assertion already written... invent neither bound... leave no blank for that task to fill"). Task 1.1b cannot be dispatched until 1.1a actually runs (which in turn needs C3 fixed first) and its two numbers are substituted in.

## C5 — TRUE

`app/dsp/Delay.hpp:679`: `widthSpread = p.dwid * baseSeconds * 0.35f * widthBalance`. `Delay.hpp:720`: `cross = p.dwid * 0.5f * widthBalance`. `widthSpread / cross = (0.35 * baseSeconds) / 0.5 = 0.7 * baseSeconds` — `p.dwid` and `widthBalance` both cancel exactly. The promoted spec (`openspec/specs/froggers-sheaf-parameter-model/spec.md:136`, carried verbatim in the delta at line 299) reads: slot 12 is "Width Balance... the ratio between the Width knob's time-offset spread and its cross-feed blend, independent of the Width knob's own value." The ratio genuinely is independent of `p.dwid` (as claimed) but it is *also* independent of `widthBalance` — the very control the requirement says produces this ratio — which is false of **today's shipped code**, before any repair. **Consequence:** blocks ARCHIVAL. This is a pre-existing defect in one of the "three MODIFIED requirements" this change carries forward and re-promotes (`One sixteen-slot bank per Froggers page`), untouched by the repair and not covered by task 3.2 (which only re-counts requirement paragraphs against promoted text, not their factual accuracy against the DSP). Archiving this change re-ships a false claim about what Width Balance's slot 12 does, unless flagged.

## C6 — TRUE (both parts)

Part 1: `ExpMapCompute(min,max,v) = min*(max/min)^v` (`app/dsp/DspMath.hpp:52-55`); at `dtim=1.0`, `ExpMapCompute(0.001,2.0,1.0)=2.0` exactly (verified: `0.001*(2000)^1.0=2.0`). `capacity = min(kMaxDelaySamples, ceil(kMaxDelaySeconds*sampleRate))` (`Delay.hpp:537`) = 96000 at 48kHz = exactly `kMaxDelaySeconds` in seconds. So `baseSeconds` equals capacity **exactly** at `dtim=1.0`, and any nonzero `widthSpread` (any nonzero `dwid`) pushes `timeR` past capacity — a stronger, more general case than the `dtim=0.99` example `proposal.md` uses. Part 2: the promoted THEN clause (`spec.md:140-141`) reads "the time-offset spread this balance produces never lengthens a read tap beyond the delay buffer's own capacity" — its grammatical subject is the *spread itself*. Clamping downstream inside `ReadAt` (`Delay.hpp:949`) leaves the spread's own computed value unbounded (still requesting more than capacity); only bounding `widthSpread` upstream makes the sentence literally true of the quantity it names. **Consequence:** blocks EXECUTION of task 1.3 as scoped — its tie-break rule ("if they differ audibly, report both and do not choose") has no clause for spec-literal conformance, so an executor could correctly pick the audibly-better option while still leaving the promoted sentence false. Needs an explicit tie-breaker or the sentence itself corrected.

## C7 — TRUE

`app/FroggersDspParityTests.cpp:6935-6949`, `stereo_delay_width_balance_mapping_keeps_cross_in_0_1_and_spread_at_or_below_todays_max`: computes `cross`/`spread` from raw local arithmetic (`dwid * 0.5f * delay.widthBalance`, a hardcoded `baseSeconds = 2.0f`) and never constructs a `DelayParams`/calls `Process()`. After the repair this test keeps asserting bounds on a formula production no longer executes. It evades task 1.4's enumeration ("every `TEST_CASE` that reaches `Process`" literally does not match it) and its before/after PASS/FAIL diff (its own state is invariant to the repair — pinned before, pinned after, unchanged). **Consequence:** blocks EXECUTION's confidence in task 1.4's stated methodology — by that task's own criteria this drift is structurally invisible, meaning task 1.4 needs to be widened (name this test explicitly for retirement/rewrite/annotation) before stage 1 can honestly claim its enumeration is complete.

## C8 & C9 — TRUE, one defect class in two locations

`Delay.hpp:615-624` (`SetWidthBalance`'s doc comment): "Because both new weights in Process() are `<literal> * widthBalance * p.dwid`... (a) cross-feed stays in [0,1]." `Delay.hpp:700-706` (comment directly above the `cross` assignment): "half of a width blend that widthBalance then scales further." Both describe the pre-repair formula; after `cross = 0.0f`, cross is no longer `<literal>*widthBalance*p.dwid` and is no longer a "width blend that widthBalance scales." Grepped `tasks.md`/`proposal.md` for `SetWidthBalance`, `bound (a)`, `bound (b)`: zero hits outside one purely descriptive line-number citation in `proposal.md`. **Consequence:** neither blocks execution nor archival by any written gate (no automated check parses C++ comment prose), but ships two confidently wrong comments in the exact file this change edits. Task 1.5 only touches the file's top provenance header, not these two sites — should be widened to cover them, consistent with this operator's own documented standard that comments must describe current behavior.

## C10 — TRUE (core claim); two supporting details need a caveat

`Delay.hpp:260-262`: "`dwid`'s cross-feed... already keeps L and R close to each other in practice" is the stated premise for choosing per-channel over linked wet limiters. The repair inverts it exactly (fixed `cross=0.0f` means the cross-feed contributes nothing to L/R proximity going forward). No task touches this comment or re-derives the per-channel-vs-linked choice. The "12000-sample steady-state window can't resolve a transient" point is sound: the check's own operating point is "12000-sample warmup discarded, 12000-sample measure" — by construction post-transient. The "grid sweeping `dtim` and `dwid`" phrasing overstates the check's actual grid, which only sweeps `dwid` — `dtim` is pinned at 0.3, Feedback at 0.7, amplitude at `0.5*uniform`. That said, the substantive conclusion is, if anything, *strengthened* by this correction: the limiter-tuning derivation's own comment (lines 68-79) required near-max feedback (`fbk≈0.98`) and *sustained full-scale* input to trigger overshoot, while this check runs at Feedback 0.7 with band-limited, half-amplitude noise — even more clearly outside the limiters' engagement region than a `dtim`-swept grid would suggest. **Consequence:** neither blocks execution nor archival of this change (limiter redesign is explicitly out of scope — a repair touching `StereoField.hpp`/`Reverb.hpp` "supersedes this proposal"), but it is a real, unflagged behavioral risk worth carrying to the operator: a design rationale has quietly gone false and nothing re-verifies it.

## C11 — TRUE

`Correlation::Value()` (`FroggersDspParityTests.cpp:9497-9501`): `return den > 0.0 ? num/den : 1.0` — an exact-silence channel makes `den=0`, reading as perfect correlation. `REQUIRE_TRUE` (`FroggersDspParityTests.cpp:57-62`) throws `std::runtime_error` on failure, unwinding the test function immediately — confirmed by reading the macro. Task 1.1b (not yet implemented) asks for an adversarial pan law and says "confirm the level-balance assertion catches it," with the correlation assertion specified first. The natural adversarial construction — silence one channel — trips the correlation assertion's degenerate 1.0 fallback first, and the level-balance assertion never executes. Since Pearson correlation is scale-invariant, the construction that actually isolates the level-balance assertion is a **pure nonzero per-channel gain scale** (never reaching exact silence), which C11 correctly identifies. **Consequence:** blocks EXECUTION of task 1.1b's own required self-proof as specified — the task should name the correct construction explicitly (a nonzero gain-scale pan law, not a silence-based one) before dispatch, or an executor will produce a "the level-balance assertion caught it" report that is true only by coincidence of assertion ordering.

## Additional findings not on the list

- **`HANDOFF.md` is itself stale in ways beyond item 4.** It still frames the open question as a single "task 1.1" (its own §"The open question, which is task 1.1"), while `tasks.md` has since split this into 1.1a/1.1b — task 2.5 already flags this exact repoint as in-scope, so it's a known, owned gap, not a new one; I confirmed the citations it names (`git show f236915^:sim/StereoDelay.hpp`) are consistent with `Delay.hpp`'s own header comment at line 47.
- **`check_modified_requirements_restate_promoted.py` and `check_docs_match_parameter_table.py` both currently pass** (`84 entries/0 failures` each document; `4 MODIFIED requirement(s), 122 promoted clause(s) restated`), confirming the baseline figures `tasks.md`/task 2.2 cite are accurate today. One `NOTE` fired, but it names `frogg3rs-midi-controller-resilience`'s own delta, not this change — consistent with that change being owned by another session and correctly excluded from this change's scope.
- I did not run the full 12-binary test suite (`nice -n10 make -j2` + per-binary runs) to re-verify the "394 passing, 0 failing" baseline in `tasks.md`, since no C-claim depended on it and a full rebuild is exactly the kind of heavy, unrequested work the subagent-sandbox rule asks to avoid absent a specific need; task 1.6's own stage gate already requires re-measuring this fresh regardless.

## Ruling: **EXECUTION IS BLOCKED**

Ordered by what must be fixed first:

1. **C3** — `task 1.1a` must state the Stereo width grid's concrete values and explicitly say whether width=0 is excluded from the max-taking that sets the bound (it must be, or the bound is provably ≥1.0 and 1.1a's own sanity check cannot pass).
2. **C4** — `task 1.1b`'s two literal `<...>` placeholders must be filled with 1.1a's actual output before dispatch; currently unrunnable as written.
3. **C11** — `task 1.1b`'s adversarial-proof instruction must specify a nonzero per-channel gain-scale pan law, not an unqualified "law that pans," or the required self-check will pass for the wrong reason.
4. **C6 (part 2)** — `task 1.3` needs an explicit tie-breaker (literal spec-sentence conformance favors bounding `widthSpread`) for the case its two candidate repairs are audibly indistinguishable.
5. **C7** — `task 1.4` must name `stereo_delay_width_balance_mapping_keeps_cross_in_0_1_and_spread_at_or_below_todays_max` explicitly (its enumeration and diff methodology both structurally miss it).
6. **C2** — `task 3.1` must be widened to rewrite the stereo-image scenario's THEN clause (not just its diagnosis and `Check:` pointer) so the promoted requirement doesn't assert monotonicity a bound-only check can't back.
7. **C8, C9** — `task 1.5` (or a new task) must cover the two stale `cross`-formula comments in `Delay.hpp` alongside the provenance header.
8. **C5** — flag to the operator before archival: the promoted "ratio... independent of the Width knob's own value" clause for slot 12 has been false of shipped code all along, unrelated to and unfixed by this repair.
9. **C10** — flag to the operator as an informational risk (out of this change's scope per its own exclusions): the per-channel-limiter design rationale is now false and unverified; no action required to land this change, but it should not be forgotten.
10. **C1** — informational only; the gate is currently green (0 failures, not 2 or 3). Recommend switching `tasks.md`'s exclusion criterion to a path-scoped rule as C1 proposes, independent of today's transient count.
